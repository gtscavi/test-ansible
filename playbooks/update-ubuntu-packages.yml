---
- name: Update Ubuntu OS
  hosts: all
  become: yes

  tasks:
      - name: Update package cache
        apt:
            update_cache: yes
        become: yes

      - name: Upgrade all packages
        apt:
            autoremove: yes
            autoclean: yes
        become: yes
